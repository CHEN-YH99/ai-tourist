# 5分钟快速部署

最简单的部署方式，适合快速上线测试。

## 准备工作

1. 一台Linux服务器（Ubuntu 20.04+）
2. OpenAI API密钥

## 一键部署脚本

### Linux/Mac 用户

```bash
# 克隆项目
git clone <你的仓库地址>
cd ai-travel-assistant

# 运行部署脚本
chmod +x quick-setup.sh
./quick-setup.sh
```

脚本会自动：
- 安装Docker（如果未安装）
- 询问OpenAI API密钥
- 自动生成安全密码
- 构建并启动所有服务
- 初始化数据库

### Windows 用户

```powershell
# 克隆项目
git clone <你的仓库地址>
cd ai-travel-assistant

# 以管理员身份运行PowerShell，然后执行
.\quick-setup.ps1
```

### 完成

部署完成后，访问 `http://你的服务器IP` 或 `http://localhost`

---

## 手动快速部署（5步）

如果脚本不可用，手动执行以下步骤：

### 步骤1：安装Docker

```bash
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER
```

重新登录或执行：
```bash
newgrp docker
```

### 步骤2：克隆项目

```bash
git clone <你的仓库地址>
cd ai-travel-assistant
```

### 步骤3：配置环境变量

```bash
# 创建配置文件
cat > .env << EOF
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=$(openssl rand -base64 32)
NODE_ENV=production
JWT_SECRET=$(openssl rand -base64 32)
OPENAI_API_KEY=你的OpenAI密钥
FRONTEND_URL=http://$(curl -s ifconfig.me)
EOF

# 创建后端配置
cat > backend/.env << EOF
NODE_ENV=production
PORT=3000
MONGODB_URI=mongodb://admin:$(grep MONGO_ROOT_PASSWORD .env | cut -d= -f2)@mongodb:27017/travel-assistant?authSource=admin
JWT_SECRET=$(grep JWT_SECRET .env | cut -d= -f2)
OPENAI_API_KEY=你的OpenAI密钥
FRONTEND_URL=http://$(curl -s ifconfig.me)
LOG_LEVEL=info
EOF
```

### 步骤4：启动服务

```bash
docker compose up -d
```

### 步骤5：初始化数据

```bash
docker compose exec backend npm run seed
```

### 完成！

访问 `http://你的服务器IP`

---

## 常用命令

```bash
# 查看状态
docker compose ps

# 查看日志
docker compose logs -f

# 重启
docker compose restart

# 停止
docker compose down

# 更新
git pull && docker compose up -d --build
```

---

## 使用云服务快速部署

### 使用Railway（最简单）

1. 访问 [railway.app](https://railway.app)
2. 连接GitHub仓库
3. 添加环境变量：
   - `OPENAI_API_KEY`
   - `JWT_SECRET`
   - `MONGODB_URI`（使用Railway的MongoDB插件）
4. 自动部署完成

### 使用Vercel + MongoDB Atlas

**前端部署到Vercel：**
```bash
npm i -g vercel
vercel --prod
```

**后端部署到Railway/Render：**
1. 连接GitHub
2. 选择 `backend` 目录
3. 添加环境变量
4. 自动部署

**数据库使用MongoDB Atlas：**
1. 注册 [mongodb.com/cloud/atlas](https://www.mongodb.com/cloud/atlas)
2. 创建免费集群
3. 获取连接字符串
4. 添加到环境变量

---

## 最小化配置（仅需3个环境变量）

```env
OPENAI_API_KEY=sk-你的密钥
JWT_SECRET=随机32位字符串
MONGODB_URI=mongodb连接字符串
```

其他配置都有默认值，可以直接运行。

---

## 故障排查

**无法访问？**
```bash
# 检查服务
docker compose ps

# 检查防火墙
sudo ufw allow 80
```

**API错误？**
```bash
# 查看日志
docker compose logs backend
```

**数据库连接失败？**
```bash
# 重启MongoDB
docker compose restart mongodb
```

---

就这么简单！🚀
