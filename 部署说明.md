# 快速部署指南

## 最简单的方式（推荐）

### Linux/Mac

```bash
git clone <你的仓库地址>
cd ai-travel-assistant
chmod +x quick-setup.sh
./quick-setup.sh
```

### Windows

```powershell
git clone <你的仓库地址>
cd ai-travel-assistant

# 方式1：使用修复后的脚本（推荐）
.\quick-setup-new.ps1

# 方式2：如果脚本有问题，使用手动部署（见下方）
```

输入OpenAI API密钥，等待3-5分钟，完成！

**注意**：如果遇到编码错误，请使用下面的"手动部署"方式。

---

## 手动部署（推荐Windows用户）

### Windows PowerShell

```powershell
# 1. 复制配置文件
Copy-Item .env.example .env
Copy-Item backend\.env.example backend\.env

# 2. 编辑配置文件（用记事本打开）
notepad .env
notepad backend\.env
# 填入你的 OPENAI_API_KEY 和其他配置

# 3. 启动服务
docker compose up -d

# 4. 等待10秒后初始化数据
Start-Sleep -Seconds 10
docker compose exec backend npm run seed
```

### Linux/Mac

```bash
# 1. 配置环境变量
cp .env.example .env
cp backend/.env.example backend/.env
# 编辑这两个文件，填入你的OpenAI API密钥

# 2. 启动服务
docker compose up -d

# 3. 初始化数据
sleep 10
docker compose exec backend npm run seed
```

访问 `http://localhost`

---

## 必需配置

只需要配置这3个：

```env
OPENAI_API_KEY=sk-你的密钥
JWT_SECRET=随机32位字符串
MONGODB_URI=mongodb://admin:密码@mongodb:27017/travel-assistant?authSource=admin
```

---

## 常用命令

```bash
docker compose ps          # 查看状态
docker compose logs -f     # 查看日志
docker compose restart     # 重启
docker compose down        # 停止
```

---

## 云平台部署

### Vercel（前端）
```bash
vercel --prod
```

### Railway（后端+数据库）
1. 连接GitHub
2. 添加环境变量
3. 自动部署

### Render（全栈）
1. 连接GitHub
2. 选择Docker部署
3. 添加环境变量

---

## 需要帮助？

查看详细文档：
- `快速部署.md` - 5分钟快速部署
- `浏览器部署教程.md` - 完整部署教程
- `README.md` - 项目说明

---

就这么简单！🚀
